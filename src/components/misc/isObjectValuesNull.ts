/**
 * Will retrun true if all values are empty, otherwise will return false id one or more values are filled.
 * 
 * @param obj Passed object for assement.
 */
export default function isObjectValuesNull(obj: Object) {
  const values = Object.values(obj);

  

}